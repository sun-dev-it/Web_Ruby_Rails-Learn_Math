<%= form_with(model: @user, url: user_path(@user), method: :patch, local: true) do |form| %>
    <%= form.file_field :avatar, class:"chosse_image_avt " %>
    <button onclick="hideDiv()">Hủy</button> 
    <%= form.submit 'Lưu' %>
<% end %>